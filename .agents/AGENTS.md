# General Rules

- Always automatically commit and `git push origin main` after completing any code modifications or tasks for the user. Do not ask for permission to push. The user has already provided their authentication token which is set in the git remote origin.
